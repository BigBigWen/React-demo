---
order: 2
title:
  zh-CN: 自定义操作
  en-US: single row custom edit
---

## zh-CN

可自定义操作项。

## en-US

custom actions in table.

```jsx
import { EditableTable } from 'hermes-react';
import { Button, Input, InputNumber, Select, Radio, Checkbox } from 'antd';

const Option = Select.Option;
const RadioGroup = Radio.Group;
const CheckboxGroup = Checkbox.Group;

const dataSource = [{
  key: '1',
  name: '胡*斌',
  gender: '男',
  age: 32,
  hobby: '吃饭',
  country: '美国'
}, {
  key: '2',
  name: '吴*祖',
  gender: '男',
  age: 42,
  hobby: '吃饭,睡觉',
  country: '中国'
}, {
  key: '3',
  name: '李*嘴',
  gender: '女',
  age: 53,
  hobby: '吃饭',
  country: '中国'
}];

const columns = [{
  title: '姓名',
  dataIndex: 'name',
  key: 'name'
},{
  title: '性别',
  dataIndex: 'gender',
  key: 'gender',
  width: 120,
  editable: true,
  formItem: (getFieldProps, id, text) => {
    let fieldProps = {
      rules: [
        { required: true, message: '请选择性别'}
      ],
      initialValue: text
    }
    return (
      <RadioGroup {...getFieldProps(id, fieldProps)}>
        <Radio value="男">男</Radio>
        <Radio value="女">女</Radio>
      </RadioGroup>
    );
  }
}, {
  title: '年龄',
  dataIndex: 'age',
  key: 'age',
  width: 100,
  editable: true,
  formItem: (getFieldProps, id, text) => {
    let fieldProps = {
      initialValue: text
    }
    return <InputNumber min={2} max={100} {...getFieldProps(id, fieldProps)} />;
  }
}, {
  title: '爱好',
  dataIndex: 'hobby',
  key: 'hobby',
  width: 150,
  editable: true,
  formItem: (getFieldProps, id, text) => {
    if (typeof text === 'string') {
      text = text.split(',')
    }
    let fieldProps = {
      initialValue: text
    }
    const plainOptions = ['吃饭', '睡觉'];
    return <CheckboxGroup 
    options={plainOptions} 
    {...getFieldProps(id, fieldProps)} />
  }
}, {
  title: '国籍',
  dataIndex: 'country',
  key: 'country',
  width: 150,
  editable: true,
  formItem: (getFieldProps, id, text) => {
    if (typeof text === 'string') {
      text = text.split(',')
    }
    let fieldProps = {
      initialValue: text
    }
    return (
      <Select {...getFieldProps(id, fieldProps)} placeholder="请选择国家">
        <Option value="中国">中国</Option>
        <Option value="美国">美国</Option>
        <Option value="日本">日本</Option>
      </Select>
    )
  }
}];

const rowEdit = {
    render: (text, record, index, edit) => {
      if (index == 1) {
        return (
          <div>
            <a href='#'>查看个人资料</a>
            <span className="ft-bar">|</span>
            {edit}
          </div>
        );
      } else if (index == 2) {
        return (
          <div>
            {edit}
            <span className="ft-bar">|</span>
            <a href='#'>打印照片</a>
          </div>
        );
      }
      return edit;
    }
};

ReactDOM.render(
  <EditableTable 
    dataSource={dataSource} 
    columns={columns} 
    pagination={false} 
    empty={true} 
    rowEdit={rowEdit}
    onSaveRow={(changedData, index) => {
      console.log('EditableTable 自定义操作，第', index, '行 内容：', changedData);
    }} 
  />
, mountNode);
```
